const rootUrl = 'http://localhost:5231/'

export const environment = {
  authApi: `${rootUrl}Auth/`,
  userApi: `${rootUrl}User/`,
  imageApi: `${rootUrl}Image/`,
  subscriptionApi: `${rootUrl}Subscription/`
};
